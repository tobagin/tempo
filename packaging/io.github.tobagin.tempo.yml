app-id: io.github.tobagin.tempo
runtime: org.gnome.Platform
runtime-version: '48'
sdk: org.gnome.Sdk
command: tempo

finish-args:
  # UI permissions (minimal for GTK4/Libadwaita)
  - --share=ipc
  - --socket=wayland
  - --socket=fallback-x11
  - --device=dri
  
  # Audio permissions (essential for metronome)
  - --socket=pulseaudio
  - --filesystem=xdg-run/pipewire-0:ro

cleanup:
  - /include
  - /lib/pkgconfig
  - /share/pkgconfig
  - /share/aclocal
  - /man
  - /share/man
  - /share/gtk-doc
  - /share/vala
  - /share/devhelp
  - '*.la'
  - '*.a'

modules:
  # Blueprint compiler (build-time only, cleaned up after)
  - name: blueprint-compiler
    buildsystem: meson
    cleanup: ['*']
    sources:
      - type: git
        url: https://gitlab.gnome.org/jwestman/blueprint-compiler.git
        tag: v1.2.2
        commit: c416c4752354c34d13b924a09f19dad90516f0a7
        
  # Main application (PyGObject already included in GNOME Platform)
  - name: tempo
    buildsystem: meson
    sources:
      - type: git
        url: https://github.com/tobagin/Tempo.git
        tag: v1.2.2
        commit: c416c4752354c34d13b924a09f19dad90516f0a7
